# Libararay_Mangement_System
Library Management System built using Java, JDBC, and MySQL to manage book records, issuing, and returns. The system includes a secure login with limited attempts and prevents a user from issuing the same book twice, ensuring accurate library transactions and efficient record management.
